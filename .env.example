# Example environment variables for Inventario-TI

# Server
PORT=3001
NODE_ENV=development

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=inventario_ti

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1d

# CORS allowed origins (comma separated). Leave empty to allow all in dev.
# Example: https://inventario.example.com,https://admin.example.com
CORS_ALLOWED=
